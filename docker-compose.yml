version: '3.8'

services:
  license-bot:
    build: .
    container_name: taptaxi-license-bot
    restart: unless-stopped
    volumes:
      - bot_data:/app/data
    env_file:
      - .env
    networks:
      - dokploy-network

networks:
  dokploy-network:
    external: true

volumes:
  bot_data:
